---
title: All Tags
description: Browse tools by category tags
---

import { getAllTags } from '@/lib/tags';
import Link from 'next/link';

export const TagsList = () => {
  const tagCounts = getAllTags();
  const sortedTags = Array.from(tagCounts.entries()).sort((a, b) =>
    a[0].localeCompare(b[0])
  );

  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 my-8">
      {sortedTags.map(([tag, count]) => (
        <Link
          key={tag}
          href={`/tags/${tag}`}
          className="block p-4 border rounded-lg hover:bg-fd-accent transition-colors"
        >
          <div className="flex items-baseline justify-between">
            <span className="font-semibold capitalize">{tag}</span>
            <span className="text-sm text-fd-muted-foreground ml-2">
              {count} {count === 1 ? 'tool' : 'tools'}
            </span>
          </div>
        </Link>
      ))}
    </div>
  );
};

# All Tags

Browse tools by category. Click on a tag to see all related tools.

<TagsList />
